export const regex = {
  passwordRegex: /^(?=.*[A-Za-z])(?=.*\d)[A-Za-z\d@$!%*?&]{6,20}$/, // 至少6位，必须包含字母和数字，可选包含常用特殊字符
  emailRegex: /^[^\s@]+@[^\s@]+\.[^\s@]+$/, // 校验邮箱格式
  usernameRegex: /^[a-zA-Z0-9_]{3,9}$/ // 至少3位，只允许字母、数字、下划线，不含特殊字符如“@”、“#”、“!
}
